name: scala-steward
on:
  push:
    branches: [ master ]
  schedule:
  - cron: '0 3 * * *'
  workflow_dispatch:
jobs:
  scala-steward:
    runs-on: ubuntu-latest
    steps:
    - name: Generate token
      id: generate_token
      uses: tibdex/github-app-token@v1
      if: ${{ !github.event.repository.fork }}
      with:
        app_id: 89839
        private_key: ${{ secrets.NSCALA_TIME_BOT_KEY }}
    - name: Launch Scala Steward
      if: ${{ !github.event.repository.fork }}
      uses: scala-steward-org/scala-steward-action@v2.13.3
      with:
        github-token: ${{ steps.generate_token.outputs.token }}
        author-email: "74924036+nscala-time-bot[bot]@users.noreply.github.com"
        author-name: nscala-time-bot[bot]
        scala-steward-version: 0.9.1-262-08e2048c-SNAPSHOT # https://oss.sonatype.org/content/repositories/snapshots/org/scala-steward/scala-steward-core_2.13/
